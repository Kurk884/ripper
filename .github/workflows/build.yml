name: Build
on: push
jobs:
build:
runs-on: windows-latest
steps:
- uses: actions/checkout@v3
- run: pip install pynput pyinstaller
- run: pyinstaller --onefile --windowed ripper.py
- uses: actions/upload-artifact@v3
with:
name: ripper
path: dist/ripper.exe
